services:
  db:
    image: postgres:15
    restart: always
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USERNAME=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=taskora
    volumes:
      - postgres-data:/var/lib/postgresql/data
volumes:
  postgres-data: